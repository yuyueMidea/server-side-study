### “强关联、可扩展”的主从表（Header–Line）设计思路

下面一套“强关联、可扩展”的主从表（Header–Line）设计思路与4个可直接落地的例子（含 PostgreSQL DDL）。每个例子都体现 1—N 关系，并适当穿插 N—N、层级、自引用与审计/状态流等常见需求。

设计总原则：
- 命名与主键：表名用单数；主键优先 uuid（或 bigserial），从表用外键引用主表。
- 通用字段：tenant_id（多租户可选）、created_at、updated_at、created_by、updated_by、is_deleted（软删可选）。
- 状态与历史：主表只存“当前状态”，配套 *_status_log 记录历史（时间、操作者、备注）。
- 金额/数量：用最小货币单位（分/厘）存整数；数量用 numeric(18,6)；加 CHECK > 0。
- 约束优先：NOT NULL、UNIQUE、CHECK、FOREIGN KEY … ON UPDATE/DELETE；常用组合唯一键（如主表ID+行号）。
- 索引：外键列、查询高频的筛选字段、时间列；必要时部分索引/联合索引。
- 可扩展字段：留 extras jsonb 存非结构化扩展，避免频繁改表。

---

**① 电商订单（orders ⇢ order_items）**

典型主从：订单头、订单行；外带支付、发货、状态流水、优惠券。
```
-- 参考：PostgreSQL
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- 订单主表
CREATE TABLE orders (
  id           uuid PRIMARY KEY DEFAULT uuid_generate_v4(),
  tenant_id    uuid,
  order_no     text UNIQUE NOT NULL,               -- 业务单号
  buyer_id     uuid NOT NULL,
  currency     char(3) NOT NULL DEFAULT 'CNY',
  total_amount bigint NOT NULL CHECK (total_amount >= 0), -- 分
  status       text NOT NULL CHECK (status IN ('CREATED','PAID','SHIPPING','DONE','CANCELLED')),
  extras       jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at   timestamptz NOT NULL DEFAULT now(),
  updated_at   timestamptz NOT NULL DEFAULT now()
);

-- 订单行（从表）
CREATE TABLE order_items (
  id           uuid PRIMARY KEY DEFAULT uuid_generate_v4(),
  order_id     uuid NOT NULL REFERENCES orders(id) ON DELETE CASCADE,
  line_no      int  NOT NULL,
  sku_id       uuid NOT NULL,
  title        text NOT NULL,
  unit_price   bigint NOT NULL CHECK (unit_price >= 0),   -- 单价(分)
  qty          numeric(18,6) NOT NULL CHECK (qty > 0),
  line_amount  bigint NOT NULL CHECK (line_amount >= 0),
  extras       jsonb NOT NULL DEFAULT '{}'::jsonb,
  UNIQUE(order_id, line_no)
);

-- 支付记录（一个订单可多次支付/退款）
CREATE TABLE payments (
  id           uuid PRIMARY KEY DEFAULT uuid_generate_v4(),
  order_id     uuid NOT NULL REFERENCES orders(id) ON DELETE CASCADE,
  pay_no       text UNIQUE NOT NULL,
  channel      text NOT NULL CHECK (channel IN ('ALIPAY','WECHAT','STRIPE','PAYPAL','OFFLINE')),
  direction    text NOT NULL CHECK (direction IN ('PAY','REFUND')),
  amount       bigint NOT NULL CHECK (amount > 0),
  status       text NOT NULL CHECK (status IN ('INIT','SUCCESS','FAIL')),
  occurred_at  timestamptz NOT NULL DEFAULT now(),
  extras       jsonb NOT NULL DEFAULT '{}'::jsonb
);

-- 发货单（主从可拆，此处简化为一表）
CREATE TABLE shipments (
  id           uuid PRIMARY KEY DEFAULT uuid_generate_v4(),
  order_id     uuid NOT NULL REFERENCES orders(id) ON DELETE CASCADE,
  ship_no      text UNIQUE NOT NULL,
  carrier      text,
  tracking_no  text,
  status       text NOT NULL CHECK (status IN ('CREATED','OUT','DELIVERED')),
  shipped_at   timestamptz,
  delivered_at timestamptz,
  extras       jsonb NOT NULL DEFAULT '{}'::jsonb
);

-- 状态流
CREATE TABLE order_status_logs (
  id           uuid PRIMARY KEY DEFAULT uuid_generate_v4(),
  order_id     uuid NOT NULL REFERENCES orders(id) ON DELETE CASCADE,
  from_status  text,
  to_status    text NOT NULL,
  reason       text,
  changed_by   uuid,
  changed_at   timestamptz NOT NULL DEFAULT now()
);

CREATE INDEX idx_order_items_order ON order_items(order_id);
CREATE INDEX idx_payments_order ON payments(order_id);
CREATE INDEX idx_shipments_order ON shipments(order_id);
CREATE INDEX idx_order_logs_order ON order_status_logs(order_id);
```
要点：
- 金额用整数（分）避免浮点误差。
- order_items 用 (order_id, line_no) 唯一约束保障“行号唯一”。
- 历史/支付/物流拆表，便于联查与扩展。

---

**② 审批流（requests ⇢ request_steps ⇢ step_actions）**

主从 + 分层子从：一个申请包含多个步骤，每步包含若干动作/意见；另有 N—N 的“抄送/候选人”。
```
CREATE TABLE requests (
  id           uuid PRIMARY KEY DEFAULT uuid_generate_v4(),
  tenant_id    uuid,
  req_no       text UNIQUE NOT NULL,
  applicant_id uuid NOT NULL,
  form_schema  jsonb NOT NULL,      -- 表单结构
  form_data    jsonb NOT NULL,      -- 表单数据
  status       text NOT NULL CHECK (status IN ('DRAFT','PENDING','APPROVED','REJECTED','CANCELLED')),
  created_at   timestamptz NOT NULL DEFAULT now(),
  updated_at   timestamptz NOT NULL DEFAULT now()
);

CREATE TABLE request_steps (
  id           uuid PRIMARY KEY DEFAULT uuid_generate_v4(),
  request_id   uuid NOT NULL REFERENCES requests(id) ON DELETE CASCADE,
  step_no      int NOT NULL,
  node_type    text NOT NULL CHECK (node_type IN ('APPROVAL','CC','AUTO')),
  approver_id  uuid,                        -- 审批人（或候选）
  status       text NOT NULL CHECK (status IN ('PENDING','PASSED','REJECTED','SKIPPED')),
  entered_at   timestamptz DEFAULT now(),
  finished_at  timestamptz,
  extras       jsonb NOT NULL DEFAULT '{}'::jsonb,
  UNIQUE(request_id, step_no)
);

CREATE TABLE step_actions (
  id           uuid PRIMARY KEY DEFAULT uuid_generate_v4(),
  step_id      uuid NOT NULL REFERENCES request_steps(id) ON DELETE CASCADE,
  action       text NOT NULL CHECK (action IN ('APPROVE','REJECT','TRANSFER','COMMENT')),
  acted_by     uuid NOT NULL,
  acted_at     timestamptz NOT NULL DEFAULT now(),
  comment      text
);

-- 抄送（N—N：request 与 user）
CREATE TABLE request_cc (
  request_id   uuid NOT NULL REFERENCES requests(id) ON DELETE CASCADE,
  user_id      uuid NOT NULL,
  PRIMARY KEY (request_id, user_id)
);

CREATE INDEX idx_steps_req ON request_steps(request_id);
CREATE INDEX idx_actions_step ON step_actions(step_id);

```
要点：
- form_schema + form_data 用 jsonb，可快速适配多种业务申请。
- 流程状态不放一步到位，靠 request_steps 与 step_actions 演进。

---

**③ 库存出入库（stock_moves ⇢ stock_move_lines）**

“主单头 + 明细行”配合“批次/库位”，再以流水表记录即时库存。
```
CREATE TABLE warehouses (
  id         uuid PRIMARY KEY DEFAULT uuid_generate_v4(),
  code       text UNIQUE NOT NULL,
  name       text NOT NULL,
  created_at timestamptz NOT NULL DEFAULT now()
);

CREATE TABLE products (
  id         uuid PRIMARY KEY DEFAULT uuid_generate_v4(),
  sku        text UNIQUE NOT NULL,
  title      text NOT NULL,
  uom        text NOT NULL DEFAULT 'EA',    -- 单位
  created_at timestamptz NOT NULL DEFAULT now()
);

-- 出入库单据（主表）
CREATE TABLE stock_moves (
  id           uuid PRIMARY KEY DEFAULT uuid_generate_v4(),
  move_no      text UNIQUE NOT NULL,
  move_type    text NOT NULL CHECK (move_type IN ('INBOUND','OUTBOUND','TRANSFER','ADJUST')),
  from_wh_id   uuid REFERENCES warehouses(id),
  to_wh_id     uuid REFERENCES warehouses(id),
  status       text NOT NULL CHECK (status IN ('DRAFT','CONFIRMED','DONE','CANCELLED')),
  remark       text,
  created_at   timestamptz NOT NULL DEFAULT now(),
  updated_at   timestamptz NOT NULL DEFAULT now(),
  CHECK (
    (move_type='INBOUND'  AND to_wh_id IS NOT NULL) OR
    (move_type='OUTBOUND' AND from_wh_id IS NOT NULL) OR
    (move_type='TRANSFER' AND from_wh_id IS NOT NULL AND to_wh_id IS NOT NULL) OR
    (move_type='ADJUST')
  )
);

-- 明细（从表）
CREATE TABLE stock_move_lines (
  id           uuid PRIMARY KEY DEFAULT uuid_generate_v4(),
  move_id      uuid NOT NULL REFERENCES stock_moves(id) ON DELETE CASCADE,
  line_no      int  NOT NULL,
  product_id   uuid NOT NULL REFERENCES products(id),
  qty          numeric(18,6) NOT NULL CHECK (qty > 0),
  lot_no       text,                        -- 批次/序列号
  UNIQUE(move_id, line_no)
);

-- 即时库存流水（累加/扣减），由业务完成后写入
CREATE TABLE stock_ledgers (
  id           uuid PRIMARY KEY DEFAULT uuid_generate_v4(),
  occurred_at  timestamptz NOT NULL DEFAULT now(),
  warehouse_id uuid NOT NULL REFERENCES warehouses(id) ON DELETE CASCADE,
  product_id   uuid NOT NULL REFERENCES products(id)   ON DELETE CASCADE,
  lot_no       text,
  delta_qty    numeric(18,6) NOT NULL,      -- 正加负减
  ref_move_id  uuid REFERENCES stock_moves(id),
  ref_line_id  uuid REFERENCES stock_move_lines(id)
);

CREATE INDEX idx_ledgers_wh_prod ON stock_ledgers(warehouse_id, product_id);
CREATE INDEX idx_move_lines_move ON stock_move_lines(move_id);
```
要点：
- CHECK 约束确保不同单据类型的仓库字段必填性。
- “即时报表”查 stock_ledgers 聚合；“明细”查 stock_move_lines。

---

**④ 预订系统（rooms ⇢ bookings）带“时间不重叠”约束**

用 Postgres 的 EXCLUDE 约束防止同一房间在同一时间段重复预订（创意实用）。
```
CREATE EXTENSION IF NOT EXISTS btree_gist;

CREATE TABLE rooms (
  id         uuid PRIMARY KEY DEFAULT uuid_generate_v4(),
  room_no    text UNIQUE NOT NULL,
  capacity   int NOT NULL CHECK (capacity > 0),
  created_at timestamptz NOT NULL DEFAULT now()
);

CREATE TABLE bookings (
  id         uuid PRIMARY KEY DEFAULT uuid_generate_v4(),
  room_id    uuid NOT NULL REFERENCES rooms(id) ON DELETE CASCADE,
  guest_id   uuid NOT NULL,
  starts_at  timestamptz NOT NULL,
  ends_at    timestamptz NOT NULL,
  status     text NOT NULL CHECK (status IN ('PENDING','CONFIRMED','CANCELLED','DONE')),
  created_at timestamptz NOT NULL DEFAULT now(),
  CHECK (ends_at > starts_at)
);

-- 同一房间时间段不允许重叠
ALTER TABLE bookings
  ADD CONSTRAINT bookings_no_overlap
  EXCLUDE USING gist (
    room_id WITH =,
    tstzrange(starts_at, ends_at, '[)') WITH &&
  ) WHERE (status IN ('PENDING','CONFIRMED'));
```
要点：
- 通过 EXCLUDE … WITH && 实现“半开区间重叠检测”。
- WHERE 子句让取消/完成的历史不参与冲突判断。
